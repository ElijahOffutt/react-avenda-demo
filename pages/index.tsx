import React from "react";

import PublicLayout from "src/layouts/public";

import { Main } from "@components";

const Home: React.FC = () => {
  return (
    <PublicLayout>
      <Main />
    </PublicLayout>
  );
};

export default Home;
